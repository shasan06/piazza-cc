from django.contrib import admin
#from .models import post, person, interaction, response
# Register your models here.






#class PostAdmin(admin.TabularInline):
    ##model = interaction


#class TweetAdmin(admin.ModelAdmin):
    #inlines = [PostAdmin]
    #list_display = ['__str__', 'post']#str is just defaulting the tweet object with id it displays the tweet with the username
    #search_fields = ['message', 'title']
    #class Meta:
        #model = post


#admin.site.register(interaction, TweetAdmin)